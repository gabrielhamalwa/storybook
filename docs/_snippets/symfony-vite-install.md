```shell renderer="symfony" packageManager="npm"
npm install --save-dev @storybook/symfony-vite
composer require --dev phloom/storybook-symfony-bundle
```

```shell renderer="symfony" packageManager="pnpm"
pnpm add --save-dev @storybook/symfony-vite
composer require --dev phloom/storybook-symfony-bundle
```

```shell renderer="symfony" packageManager="yarn"
yarn add --dev @storybook/symfony-vite
composer require --dev phloom/storybook-symfony-bundle
```
