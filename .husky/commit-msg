if [ -z "$SKIP_STORYBOOK_GIT_HOOKS" ]; then
  if grep -iE 'Generated with \[Devin\]|Co-Authored-By:.*Devin|Co-authored-by:.*Devin' "$1"; then
    echo "Error: Commit message contains Devin AI attribution lines."
    echo "Remove them before committing."
    exit 1
  fi
fi
